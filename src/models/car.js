export default class Car {
  constructor(name, description, number, price, numberOfStocks, address) {
    this.name = name;
    this.description = description;
    this.number = number;
    this.price = price;
    this.numberOfStocks = numberOfStocks;
    this.address = address;
  }
}
